# The Prisma layer

To maintain the project structure and readability, each entity should exist in the /models folder, and when you are ready just regenerate the datamodel.

```bash
python generate.py
```

